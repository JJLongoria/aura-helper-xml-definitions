describe('Testing src/definitions/queueRoutingConfig.js', () => {
    test('Testing Definition', () => {
        const QueueRoutingConfig = require('../../../src/definitions/queueRoutingConfig');
    });
});