describe('Testing src/definitions/dataSourceObject.js', () => {
    test('Testing Definition', () => {
        const DataSourceObject = require('../../../src/definitions/dataSourceObject');
    });
});