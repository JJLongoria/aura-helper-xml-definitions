describe('Testing src/definitions/embeddedServiceConfig.js', () => {
    test('Testing Definition', () => {
        const EmbeddedServiceConfig = require('../../../src/definitions/embeddedServiceConfig');
    });
});