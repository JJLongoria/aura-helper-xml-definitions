describe('Testing src/definitions/chatterExtension.js', () => {
    test('Testing Definition', () => {
        const ChatterExtension = require('../../../src/definitions/chatterExtension');
    });
});