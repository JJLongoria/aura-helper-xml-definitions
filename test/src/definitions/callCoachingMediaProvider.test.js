describe('Testing src/definitions/callCoachingMediaProvider.js', () => {
    test('Testing Definition', () => {
        const CallCoachingMediaProvider = require('../../../src/definitions/callCoachingMediaProvider');
    });
});