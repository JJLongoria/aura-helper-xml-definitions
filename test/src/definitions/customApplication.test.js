describe('Testing src/definitions/customApplication.js', () => {
    test('Testing Definition', () => {
        const CustomApplication = require('../../../src/definitions/customApplication');
    });
});