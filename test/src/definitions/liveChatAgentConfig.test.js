describe('Testing src/definitions/liveChatAgentConfig.js', () => {
    test('Testing Definition', () => {
        const LiveChatAgentConfig = require('../../../src/definitions/liveChatAgentConfig');
    });
});