const PicklistValuesType = require('../../../../src/definitions/types/picklistValues');

describe('Testing src/definitions/types/packageVersion.js', () => {
    test('Testing Definition', () => {
        let result = PicklistValuesType('test', 'Test', 30);
    });
});