const MlIntentType = require('../../../../src/definitions/types/mlIntent');

describe('Testing src/definitions/types/mlIntent.js', () => {
    test('Testing Definition', () => {
        let result = MlIntentType('mlIntentTest', 'ML Intent Test', 30);
    });
});