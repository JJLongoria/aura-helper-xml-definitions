const KeyboardShortcutsType = require('../../../../src/definitions/types/keyboardShortcuts');

describe('Testing src/definitions/types/keyboardShortcuts.js', () => {
    test('Testing Definition', () => {
        let result = KeyboardShortcutsType('test', 'Test', 40);
    });
});