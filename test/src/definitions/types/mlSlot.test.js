const MlSlotType = require('../../../../src/definitions/types/mlSlot');

describe('Testing src/definitions/types/mlSlot.js', () => {
    test('Testing Definition', () => {
        let result = MlSlotType('mlSlotTest', 'ML Slot Test', 30);
    });
});