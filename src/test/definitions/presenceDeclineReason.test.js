describe('Testing src/definitions/presenceDeclineReason.js', () => {
    test('Testing Definition', () => {
        const PresenceDeclineReason = require('../../definitions/presenceDeclineReason');
    });
});