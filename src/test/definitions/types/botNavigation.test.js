const BotNavigationType = require('../../../definitions/types/botNavigation');

describe('Testing src/definitions/types/botNavigation.js', () => {
    test('Testing Definition', () => {
        let result = BotNavigationType('test', 'test', 42);
    });
});