const filterItemType = require('../../../definitions/types/filterItem');

describe('Testing src/definitions/types/filterItem.js', () => {
    test('Testing Definition', () => {
        let result = filterItemType('testFilterItem', 'Test Filter Item');
    });
});