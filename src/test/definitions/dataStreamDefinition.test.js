describe('Testing src/definitions/dataStreamDefinition.js', () => {
    test('Testing Definition', () => {
        const DataStreamDefinition = require('../../definitions/dataStreamDefinition');
    });
});