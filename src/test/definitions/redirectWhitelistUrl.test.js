describe('Testing src/definitions/redirectWhitelistUrl.js', () => {
    test('Testing Definition', () => {
        const RedirectWhitelistUrl = require('../../definitions/redirectWhitelistUrl');
    });
});