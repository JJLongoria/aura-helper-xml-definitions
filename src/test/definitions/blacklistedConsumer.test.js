describe('Testing src/definitions/blacklistedConsumer.js', () => {
    test('Testing Definition', () => {
        const BlacklistedConsumer = require('../../definitions/blacklistedConsumer');
    });
});