describe('Testing src/definitions/profilePasswordPolicy.js', () => {
    test('Testing Definition', () => {
        const ProfilePasswordPolicy = require('../../definitions/profilePasswordPolicy');
    });
});