describe('Testing src/definitions/userProvisioningConfig.js', () => {
    test('Testing Definition', () => {
        const UserProvisioningConfig = require('../../definitions/userProvisioningConfig');
    });
});