
describe('Testing src/definitions/aIApplicationConfig.js', () => {
    test('Testing Definition', () => {
        const AIApplicationConfig = require('../../definitions/aIApplicationConfig');

    });
});